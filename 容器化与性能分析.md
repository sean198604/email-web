# 邮件群发工具 · 容器化与性能/稳定性分析

> 文档版本：v1.0
> 日期：2026-07-18
> 关联文档：`项目说明.md`
> 端口变更：应用统一改为 **5051**（开发 `/ 生产`）

---

## 一、能不能容器化？结论：**可以，且非常合适**

本项目是一个标准的 **Next.js 全栈应用 + SQLite 文件数据库**，没有 Windows 专属依赖、没有 GUI、没有外部重型中间件，完全满足容器化条件。

| 判定项 | 结果 | 说明 |
|---|---|---|
| 是否跨平台 | ✅ | Node.js 运行时，可在 Linux 容器运行 |
| 是否有系统级依赖 | ⚠️ 可控 | `better-sqlite3` 是原生模块，已在 Dockerfile 内置编译工具链，构建时自动编译/拉取预编译包 |
| 数据库是否可嵌 | ✅ | SQLite 为单文件，用数据卷持久化即可 |
| 是否有状态 | ✅（SQLite） | 状态在 `dev.db`，通过 `volume` 挂载保证不随容器丢失 |
| 是否需要特权 | ❌ | 普通非 root 进程即可运行 |

已交付的容器化文件：
- `Dockerfile` — 多阶段构建（deps / builder / runner），生产镜像约 600–800MB
- `docker-compose.yml` — 端口映射 `5051:5051`、数据卷、自动重启、健康检查
- `.dockerignore` — 排除 `node_modules`/`.next`/`.env` 等，缩小构建上下文

---

## 二、性能会不会更好？结论：**基本持平，不会魔幻加速**

### 2.1 原始性能
Docker 在 Linux 原生 / WSL2 下对 CPU 与网络的开销 **< 2–3%**，几乎可忽略。本应用是 **I/O 密集型**（SMTP 发信 + SQLite 读写），CPU 不是瓶颈，因此容器化**不会显著提升也不会明显下降**吞吐量。

| 维度 | 裸机/直接运行 | Docker 容器 |
|---|---|---|
| 接口响应延迟 | 基准 | ≈ 基准（±2%） |
| SMTP 发信速度 | 基准 | 完全一致（瓶颈在邮件服务商） |
| SQLite 读 | 基准 | 一致 |
| SQLite 写 | 基准 | 取决于存储位置（见下） |
| 冷启动 | 无 | 仅首次加载镜像层，一次性 |

### 2.2 唯一可能变慢的坑：SQLite 跨虚拟机挂载
若在本机用 **Docker Desktop（Windows/macOS）** 并把宿主目录 bind-mount 进容器（`-v /c/...:/app/data`），SQLite 的每次 `fsync` 都要穿越 VM 边界，**写入会明显变慢**（可能 3–10 倍）。

**规避方式（本项目已采用）**：用 **命名数据卷（named volume）** `email_data:/app/data`，数据库落在容器内部存储，避免跨 VM 惩罚 → 性能回到原生水平。

> ✅ 结论：只要用数据卷而非宿主机 bind-mount，性能与原生**基本无差**；容器化本身不提供加速。

---

## 三、稳定性会不会更好？结论：**部署/运维稳定性明显更好**

容器化改善的是**运行环境与运维**，而非算法本身：

| 收益 | 说明 |
|---|---|
| 环境可复现 | 镜像锁定 Node 22 + 全部依赖，杜绝"我机器上能跑" |
| 自动恢复 | `restart: unless-stopped` 进程崩溃后自动拉起 |
| 健康检查 | `healthcheck` 探测 `/login`，异常可触发编排重启 |
| 资源隔离/限制 | 可设 CPU/内存上限，避免发信任务跑满宿主内存导致 OOM |
| 原子回滚 | 镜像打 tag，出问题一键回退上一版本 |
| 依赖隔离 | 不与宿主其他 Node 项目冲突 |

### 3.1 需要注意的边界
- **单实例 SQLite**：SQLite 不支持高并发多写。本项目发信走内存队列**串行发送**，单容器完全没问题；但若要**横向扩成多个副本**，SQLite 会因写锁失败 → 此时应切换为 PostgreSQL（Prisma 一行 `provider` 切换即可）。
- **数据落盘**：必须挂卷，否则容器删除即数据清零（本项目已配置 `email_data` 卷）。

> ✅ 结论：对"部署后无人值守"场景，稳定性**显著优于**直接 `npm run start` 跑在宿主机。

---

## 四、端口变更：3001 / 3000 → 5051

- `package.json`：`dev` → `next dev -p 5051`，`start` → `next start -p 5051`
- `Dockerfile`：`ENV PORT=5051` + `EXPOSE 5051`
- `docker-compose.yml`：`"5051:5051"`
- 访问地址：开发 `http://localhost:5051`；容器 `http://<host>:5051/login`

---

## 五、快速上手

### 本地直接运行（开发）
```bash
cd D:/EMAIL/email-web
npm install
npx prisma migrate dev
npm run dev          # 访问 http://localhost:5051
```

### Docker 运行（生产）
```bash
cd D:/EMAIL/email-web
docker compose up -d --build     # 首次构建并后台启动
docker compose ps                # 查看状态/健康
docker compose logs -f           # 看日志
# 访问 http://localhost:5051/login
```
- 数据位于 Docker 卷 `email_data`，升级镜像不会丢数据
- 停止：`docker compose down`（保留数据）；`docker compose down -v`（连数据卷一起删）

### 手动 docker run（不依赖 compose）
```bash
docker build -t email-web:latest .
docker run -d --name email-web -p 5051:5051 \
  -v email_data:/app/data \
  -e DATABASE_URL="file:/app/data/dev.db" \
  --restart unless-stopped email-web:latest
```

---

## 六、镜像优化建议（可选）

当前 Dockerfile 采用**完整 node_modules + `next start`**，可靠性最高，代价是镜像偏大。如需更小更快的镜像，可启用 Next.js 官方推荐的 `output: 'standalone'`：
- 在 `next.config.ts` 加 `output: 'standalone'`
- Dockerfile 改为只复制 `.next/standalone` + `.next/static` + `public`，并显式补拷 `node_modules/@prisma`、`node_modules/better-sqlite3`、`src/generated`（避免原生模块未被追踪而缺失）

> 对内部工具，**当前方案优先稳定性**，无需强行瘦身；上述 standalone 方案仅在镜像分发/CI 频繁拉取时有明显收益。

---

## 七、一句话总结

**能容器化、应该容器化；性能与原生基本持平（用数据卷避免 SQLite 跨 VM 变慢），运维稳定性显著更好；端口已统一改为 5051，开箱即跑。**
