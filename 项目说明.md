# 邮件群发工具 · 网页版 · 项目技术说明

> 文档版本：v1.0
> 生成日期：2026-07-17
> 项目路径：`D:/EMAIL/email-web/`
> 定位：将一款 NSIS 封装的 Electron/Chromium 桌面邮件群发工具，复现为**网页版 SaaS 形态**的纯邮件发送系统（已移除原版软件激活/卡密模块）。

---

## 一、项目概述

本项目是对一款商业桌面软件（邮件群发工具 v5.2.3）的逆向分析与网页版复现。原版为 Electron + Chromium + Node.js + React 技术栈，通过 NSIS 安装器分发；本复现版保留其核心业务能力，并将其改造为**多用户隔离**的网页应用，技术上替换为业界主流的 Next.js 全栈方案。

### 核心目标
- ✅ 完整复现原版邮件群发业务功能（联系人、模板、群发任务、账号、日志、黑名单等）
- ✅ 网页化：浏览器即可使用，无需安装
- ✅ 多用户隔离：每个注册用户拥有独立数据空间
- ✅ 纯邮件工具：聚焦发送能力，剔除授权/激活等商业化模块

---

## 二、技术架构

### 2.1 整体分层

```
┌─────────────────────────────────────────────┐
│  浏览器（Client）                            │
│  React 19 + Tailwind + shadcn/ui 组件        │
│  路由守卫 / 登录态 / 数据展示与交互           │
└───────────────┬─────────────────────────────┘
                │  HTTPS / REST API
┌───────────────▼─────────────────────────────┐
│  Next.js 16（App Router，服务端）            │
│  ├─ API Routes（业务逻辑 / 鉴权过滤）       │
│  ├─ Middleware（登录态校验 / 路由守卫）      │
│  ├─ Auth（JWT + bcrypt 密码哈希）           │
│  ├─ Mailer（nodemailer 发信）               │
│  └─ Sender（群发队列 / 账号轮发策略）       │
└───────────────┬─────────────────────────────┘
                │  Prisma 7 ORM
┌───────────────▼─────────────────────────────┐
│  SQLite（@prisma/adapter-better-sqlite3）    │
│  文件：dev.db（用户数据完全隔离）            │
└──────────────────────────────────────────────┘
```

### 2.2 技术栈清单

| 层 | 技术 | 版本 | 用途 |
|---|---|---|---|
| 框架 | Next.js（App Router） | 16.2.10 | 全栈 React 框架，SSR + API 一体 |
| 前端语言 | React | 19.2.4 | UI 渲染 |
| 样式 | Tailwind CSS | 4 | 原子化 CSS |
| 组件库 | shadcn/ui（基于 @base-ui/react） | — | 高质量可定制 UI 组件 |
| 图标 | lucide-react | 1.24.0 | 矢量图标 |
| 表格 | @tanstack/react-table | 8.21.3 | 数据表格（排序/筛选） |
| 图表 | recharts | 3.9.2 | 仪表盘趋势图 |
| 动画 | framer-motion | 12.42.2 | 过渡动画 |
| 状态管理 | zustand | 5.0.14 | 轻量全局状态 |
| 通知 | sonner | 2.0.7 | Toast 提示 |
| ORM | Prisma | 7.8.0 | 数据库访问 |
| 数据库驱动 | @prisma/adapter-better-sqlite3 + better-sqlite3 | 7.8.0 / 12.11.1 | SQLite 适配 |
| 数据库 | SQLite | — | 嵌入式文件数据库 |
| 发信 | nodemailer | 9.0.3 | SMTP 邮件发送 |
| 认证 | jose（JWT） | 6.2.3 | 令牌签发/校验 |
| 密码哈希 | bcryptjs | 3.0.3 | 密码安全存储 |
| 数据解析 | papaparse / xlsx | 5.5.4 / 0.18.5 | CSV / Excel 导入 |
| 编码处理 | chardet / iconv-lite | 2.2.0 / 0.7.3 | 文件编码识别与转换 |
| 拖拽 | @dnd-kit | 6.x | 分组/排序交互 |
| 语言 | TypeScript | 5 | 类型安全 |

### 2.3 与原版（Electron）的技术映射

| 原版（桌面端） | 网页版替代方案 |
|---|---|
| better-sqlite3 本地 DB | Prisma 7 + SQLite（服务端托管） |
| Node.js worker_threads 发信 | Next.js API Route + 内存队列（`activeTasks` Map）+ 异步发送 |
| IPC 进程通信 | REST API |
| 机器码绑定激活 | 用户注册 + JWT + httpOnly Cookie（已按需求移除激活模块） |
| 原生文件系统 | 浏览器 File API + 服务端解析 |
| TinyMCE 富文本 | 模板 HTML 编辑（textarea + 变量占位符） |

---

## 三、功能模块

系统共 **9 个业务页面 + 登录/注册页**，对应 21 个 API 路由。

### 3.1 模块清单

| # | 模块 | 路径 | 核心能力 |
|---|---|---|---|
| 1 | 仪表盘 | `/` | 统计卡片（联系人总数/发件账号/今日发送/活跃任务）、7 天发送趋势图、最近发送记录 |
| 2 | 联系人管理 | `/contacts` | 列表/搜索/批量勾选、分组管理（增删改/合并）、标签体系、CSV/Excel 批量导入、手动添加 |
| 3 | 邮件模板 | `/templates` | 模板增删改查、HTML + 纯文本双版本、变量占位符、分类、克隆 |
| 4 | 群发任务 | `/tasks` | 创建向导（选模板+分组+账号+策略+计划）、启动/暂停/停止、进度条、发送记录 |
| 5 | 账号管理 | `/accounts` | 多 SMTP 账号管理、轮发策略（轮询/权重/智能）、日/时限额、发送间隔、连接测试 |
| 6 | 发送日志 | `/logs` | 成功/失败记录、错误原因、筛选/搜索、导出、清空 |
| 7 | 黑名单 | `/blacklist` | 邮箱级 + 域名级屏蔽、批量添加、原因备注 |
| 8 | 设置 | `/settings` | 外观主题、数据备份/恢复、垃圾敏感词配置、退订链接与页脚 HTML |
| 9 | 登录/注册 | `/login` `/register` | 用户注册、登录、登出、会话保持 |

### 3.2 发信引擎设计（`src/lib/sender.ts` + `src/lib/mailer.ts`）

- **账号轮发策略**：支持 `round-robin`（轮询）、`weighted`（权重）、`optimal`（智能）三种策略，按当日剩余限额（`dailyLimit - todaySent`）调度。
- **限流控制**：每个账号支持 `dailyLimit`、`hourlyLimit`、`sendInterval`（发送间隔毫秒），防止触发服务商风控。
- **内存队列**：以 `Map<taskId, boolean>` 维护活动任务状态，避免重复触发。
- **任务状态机**：`draft → pending → sending → completed / failed`，支持暂停/停止。
- **黑名单过滤**：发送前对联系人邮箱/域名做黑名单比对，自动跳过。
- **垃圾词检测**：`spam-detector.ts` 内置敏感词引擎，发送前可做合规检查。
- **变量替换**：模板中 `{name}` 等占位符按联系人 `variables` 字段个性化渲染。

### 3.3 认证与用户隔离

- **注册**：邮箱 + 密码（bcryptjs 哈希存储），自动创建隔离数据空间。
- **登录**：签发 JWT（jose，HS256，7 天有效期），写入 httpOnly Cookie（`email-token`）。
- **路由守卫**：`middleware.ts` 拦截受保护页面，未登录统一跳转 `/login`。
- **数据隔离**：9 张业务表均含 `userId` 外键，所有 API 查询强制 `where: { userId }`，确保 A 用户无法读取 B 用户数据。
- **级联删除**：用户删除时其账号/联系人/任务等通过 `onDelete: Cascade` 一并清理。

---

## 四、数据模型

数据库共 **10 张表**，全部以 `userId` 实现多租户隔离：

| 表 | 说明 | 关键字段 |
|---|---|---|
| `User` | 用户账户 | email(唯一)、password、role |
| `Account` | SMTP 发件账号 | host/port/secure/authUser/authPass、dailyLimit/hourlyLimit/sendInterval/weight、todaySent |
| `ContactGroup` | 联系人分组 | name、description |
| `Contact` | 联系人 | name/email、tags(JSON)、variables(JSON)、groupId |
| `Template` | 邮件模板 | subject、htmlContent、textContent、category、variables(JSON) |
| `Task` | 群发任务 | status、templateId、groupId、accountIds(JSON)、strategy、scheduledAt、sentCount/failCount |
| `SendRecord` | 发送记录 | taskId、contactEmail、accountEmail、status、errorMessage |
| `Blacklist` | 黑名单 | value、type(email/domain)、reason |
| `AuditLog` | 操作日志 | action、detail、level |
| `Setting` | 系统设置 | data(JSON) |

> 注：`tags`、`variables`、`accountIds`、`attachments` 等列表型字段以 JSON 字符串存储，避免额外关联表，契合轻量场景。

---

## 五、关键实现要点

1. **Prisma 7 适配**：新版 Prisma 强制要求 Driver Adapter，项目采用 `@prisma/adapter-better-sqlite3`，`db.ts` 统一通过 `DATABASE_URL` 配置路径。
2. **Generated Client 路径**：`./src/generated/prisma`，全项目统一引用，避免 `@prisma/client` 与生成路径不一致。
3. **开发指示器关闭**：`next.config.ts` 中 `devIndicators: false`，隐藏左下角 Next.js 标识。
4. **EGO 品牌集成**：`public/logo.png` 展示于侧边栏与登录/注册页，区分于原版无品牌状态。
5. **批量导入**：`/api/contacts/import` 支持 JSON 数组批量写入，并自动附带 `userId`。
6. **附件与页脚**：任务支持 `attachments` 数组与 `footerHtml` 页脚（含退订链接开关），满足合规发送。

---

## 六、运行与部署

### 本地开发
```bash
cd D:/EMAIL/email-web
npm install            # 安装依赖
npx prisma migrate dev # 初始化数据库
npm run dev            # 启动开发服务器（默认 3000，当前实例 3001）
```
- 访问：登录页 `http://localhost:3001/login`
- 数据库文件：`D:/EMAIL/email-web/dev.db`

### 生产构建
```bash
npm run build
npm run start
```

### API 路由总览（21 个）
```
/auth/register | /auth/login | /auth/logout | /auth/me
/accounts  | /accounts/[id]
/contacts  | /contacts/[id] | /contacts/import
/groups    | /groups/[id]
/templates | /templates/[id]
/tasks     | /tasks/[id]
/logs
/blacklist | /blacklist/[id]
/settings
/stats
/send/test
```

---

## 七、与原版能力对照（已复现 / 差异）

| 能力 | 原版 | 网页版 | 状态 |
|---|---|---|---|
| 多 SMTP 账号 + 轮发 | ✅ | ✅ | 已实现 |
| 联系人分组/标签/导入 | ✅ | ✅ | 已实现 |
| 模板 HTML + 变量 | ✅ | ✅ | 已实现 |
| 群发任务 + 定时 | ✅ | ✅（定时字段已建，调度器可用） | 已实现 |
| 发送日志 + 审计 | ✅ | ✅ | 已实现 |
| 黑名单 | ✅ | ✅ | 已实现 |
| 垃圾词检测 | ✅ | ✅ | 已实现 |
| 仪表盘统计 | ✅ | ✅ | 已实现 |
| 多用户隔离 | ❌（单机） | ✅ | 增强 |
| 软件激活/卡密 | ✅ | ❌ | 按需求移除 |
| 富文本编辑器(TinyMCE) | ✅ | ⚠️ 简化为 HTML 文本编辑 | 可后续增强 |
| 真实 SMTP 连接测试 | ✅ | ✅ | 已实现 |

---

## 八、后续可扩展方向

- 群发任务**定时调度器**（基于 `scheduledAt` 的 cron 或轮询触发）
- 模板 **TinyMCE / 可视化富文本编辑器**接入
- 附件**真实上传与存储**（当前为路径字段）
- 发送**速率自适应 / 熔断**（根据失败率自动降速）
- 邮件**打开率 / 点击率追踪**（像素/短链）
- 数据库可切换 PostgreSQL 以支持更高并发与多实例部署

---

*本文档基于项目源码（`prisma/schema.prisma`、`package.json`、`src/lib/*`、`src/app/*`）实测整理，构建已通过，认证隔离已验证。*
