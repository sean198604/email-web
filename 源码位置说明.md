# email-web（端口 5051）源码位置说明

> 编写日期：2026-07-21
> 适用版本：email-web 0.1.0 / Next.js 16 / Docker 镜像 `email-web:latest`
> 结论：源码没有丢，它就在本地磁盘 **`D:\EMAIL\email-web\`**。

---

## 一、一句话结论

**源码从未丢失，完整保存在 `D:\EMAIL\email-web\`。** 这是一个标准的 Next.js 16 项目根目录（含 `src/`、`prisma/`、`package.json`、`Dockerfile`、`docker-compose.yml`、`.git/` 等）。当前运行中的 Docker 容器 `email-web` 正是由该目录 `docker build` 构建而来。

你之前"找不到源码"，通常是以下原因之一：
- 在 `D:\EMAIL` **根目录**找，但源码实际在子目录 **`email-web`** 里；
- 误以为代码只存在于 Docker **容器内**，容器重建后以为源码没了（其实容器只是镜像的运行实例，源码在宿主机）；
- 看到 `.git` 仓库里"缺文件"（依赖/缓存/生成物被忽略），误判为源码不全。

---

## 二、快速验证（源码确实在场）

| 验证项 | 路径 | 说明 |
|---|---|---|
| 项目根目录 | `D:\EMAIL\email-web` | 含 package.json、src、prisma、Dockerfile 等 |
| 前端页面/API 源码 | `D:\EMAIL\email-web\src\app` | 仪表盘、登录、联系人、模板、任务、账号、日志、黑名单、设置、管理员页 + `api/*` |
| 数据库模型 | `D:\EMAIL\email-web\prisma\schema.prisma` | 10 张业务表 + User 定义 |
| 构建/部署配置 | `D:\EMAIL\email-web\Dockerfile`、`docker-compose.yml` | 容器化定义 |
| 本地 Git 仓库 | `D:\EMAIL\email-web\.git` | 该目录自带仓库（注意下文"Git 忽略项"） |
| 运行中的容器 | `docker ps` 中 `email-web`（image: `email-web:latest`） | 由上方目录 `docker build .` 产出 |

---

## 三、为什么会"看起来缺失"（澄清 4 个误解）

1. **目录层级误解** —— 源码在 `D:\EMAIL\email-web`，不是 `D:\EMAIL` 根。根目录下还有 `app_contents/`、`extracted_app/`、`邮件群发工具-v5.2.3.exe` 等**原桌面版（Electron）残留文件**，容易让人误以为"web 源码不在这里"。

2. **容器 ≠ 源码** —— 运行中的容器 `email-web` 内确实有代码，但那是**构建镜像时 COPY 进去的副本**，并非源码来源。重建镜像（`docker compose build`）读取的就是 `D:\EMAIL\email-web` 这个目录（build context）。删除容器/镜像都**不会**影响宿主机源码。

3. **Git 忽略项造成的"缺文件"错觉** —— `.gitignore` 忽略了 `node_modules/`、`/.next/`、`src/generated/prisma`、`.env*`、`*.tsbuildinfo`。所以一个"干净"的 git 仓库里**看不到**依赖、构建缓存、Prisma 生成的客户端和密钥文件。但这些文件在**当前工作目录里都真实存在**（已 `npm ci` 安装、已 `npx prisma generate` 生成）。他人 `clone` 你的仓库后会缺这些，需重新生成（见第五节）。

4. **本地 `dev.db` 不是运行库** —— 目录里有个 `dev.db`（早期本地调试残留），但**生产容器使用的是 Docker 命名卷 `email-web_email_data`**（容器内 `/app/data/dev.db`）。两者并非同一文件：改本地 `dev.db` 不会影响线上数据，反之亦然。线上真实数据在卷里，重建/重启容器不丢。

---

## 四、项目结构（源码根）

```
D:\EMAIL\email-web\
├─ src\
│  ├─ app\                      # 页面 + API 路由（App Router）
│  │  ├─ page.tsx               # 仪表盘
│  │  ├─ login\  register\      # 认证页
│  │  ├─ contacts\  templates\  tasks\  accounts\
│  │  ├─ logs\  blacklist\  settings\  admin\
│  │  └─ api\                   # 全部后端接口（auth/contacts/tasks/upload/...）
│  ├─ components\               # sidebar、theme-provider、ui/*（shadcn/base-ui）
│  ├─ lib\                      # auth.ts / db.ts / mailer.ts / sender.ts / theme.ts / spam-detector.ts
│  ├─ middleware.ts             # 登录态守卫（未登录跳 /login）
│  └─ generated\prisma\         # Prisma 生成的客户端（git 忽略，需 generate）
├─ prisma\
│  ├─ schema.prisma             # 数据模型（10 张业务表 + User，均带 userId 外键）
│  └─ migrations\               # 已落地的 SQL 迁移
├─ public\                      # 静态资源
├─ prisma.config.ts             # Prisma 7 datasource 配置（url 在此，非 schema 内）
├─ Dockerfile                   # 多阶段构建（deps → builder → runner）
├─ docker-compose.yml           # 端口 5051 + 命名卷 + 环境变量 + healthcheck
├─ package.json                 # scripts: dev/build/start 均固定端口 5051
└─ .git\                        # 本地 Git 仓库
```

---

## 五、三种"找回 / 运行"方式

### A. 直接本地开发（改代码最快）
```bash
cd D:\EMAIL\email-web
npm run dev          # 启动开发服务器，访问 http://localhost:5051
```
> 需已 `npm ci` 且本地 `dev.db` 存在。本地 dev 用的是目录内 `dev.db`（非线上卷）。

### B. 重新构建 Docker 镜像并部署（生产）
```bash
cd D:\EMAIL\email-web
docker compose build          # 用当前目录作为 build context 重建镜像
docker compose up -d          # 启动，端口 5051，数据写入命名卷 email-web_email_data
```
> 容器启动命令为 `npx prisma migrate deploy && npm run start`，迁移自动落地；`Dockerfile` 已内含 `npm ci` 与 `npx prisma generate`。

### C. 仅备份 / 恢复数据（不动源码）
```powershell
# 备份线上数据库（命名卷）
docker compose cp email-web:/app/data/dev.db D:/EMAIL/db_backups/dev_backup_<时间戳>.db
```

---

## 六、注意事项

- ⚠️ **改代码后部署**：务必在 `D:\EMAIL\email-web` 里改源码，再 `docker compose build` 才会生效。在容器内临时 `vi` 修改的内容会随容器销毁丢失。
- ⚠️ **clone 到新机器**：`git clone` 后需先 `npm ci` 再 `npx prisma generate`，否则缺依赖与 Prisma 客户端。Docker 构建流程已内含这两步，无需手动。
- ⚠️ **密钥安全**：`docker-compose.yml` 与 `.env` 内含 `JWT_SECRET`、`ADMIN_PASSWORD` 明文（`.env` 已被 `.gitignore` 忽略，compose 中为明文），请勿提交到公开仓库。
- 更完整的架构与设计背景见同目录 `项目说明.md`；容器化与性能分析见 `容器化与性能分析.md`。

---

## 七、常用命令速查

| 目的 | 命令 |
|---|---|
| 本地启动 | `cd D:\EMAIL\email-web && npm run dev` |
| 生产构建 + 起服 | `cd D:\EMAIL\email-web && docker compose up -d --build` |
| 查看容器 | `docker ps \| findstr email` |
| 进容器调试 | `docker exec -it email-web sh` |
| 重新生成 Prisma 客户端 | `cd D:\EMAIL\email-web && npx prisma generate` |
| 备份线上库 | `docker compose cp email-web:/app/data/dev.db D:/EMAIL/db_backups/xxx.db` |
| 进源码目录 | `explorer D:\EMAIL\email-web` |
```
